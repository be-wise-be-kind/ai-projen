# Purpose: Example Terraform variable values for workspace configuration
# Scope: Sample values for all workspace types (VPC, ECS, ALB)
# Overview: This file provides example variable values for configuring Terraform workspaces.
#     Copy this file to terraform.tfvars in your workspace directory and customize the
#     values for your specific environment. Different workspaces require different
#     variables - see comments below for workspace-specific configurations.
# Dependencies: None
# Configuration: Copy to terraform.tfvars and customize for your project
# Implementation: One tfvars file per workspace and environment

# =============================================================================
# COMMON VARIABLES (used by all workspaces)
# =============================================================================

project_name = "myproject"
environment  = "dev"  # Options: dev, staging, prod
aws_region   = "us-west-2"

additional_tags = {
  Team       = "platform"
  CostCenter = "engineering"
  Owner      = "ops-team"
}

# =============================================================================
# VPC WORKSPACE VARIABLES
# =============================================================================
# Use these variables when configuring the VPC workspace

# vpc_cidr = "10.0.0.0/16"
# az_count = 2
# app_port = 8000

# =============================================================================
# ECS WORKSPACE VARIABLES
# =============================================================================
# Use these variables when configuring the ECS workspace

# service_name         = "backend"
# container_image      = "123456789012.dkr.ecr.us-west-2.amazonaws.com/myproject-backend:latest"
# container_port       = 8000
# task_cpu             = "256"
# task_memory          = "512"
# desired_count        = 1
# terraform_state_bucket = "myproject-123456789012-terraform-state"

# # Container environment variables
# container_environment = [
#   {
#     name  = "ENVIRONMENT"
#     value = "dev"
#   },
#   {
#     name  = "LOG_LEVEL"
#     value = "INFO"
#   }
# ]

# # Health check configuration
# health_check_command = ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]

# # Auto-scaling configuration (production only)
# enable_autoscaling       = false
# autoscaling_min_capacity = 1
# autoscaling_max_capacity = 4
# autoscaling_cpu_target   = 60.0

# # Target group ARN from ALB workspace (optional)
# target_group_arn = ""

# =============================================================================
# ALB WORKSPACE VARIABLES
# =============================================================================
# Use these variables when configuring the ALB workspace

# service_name                    = "api"
# target_port                     = 8000
# terraform_state_bucket          = "myproject-123456789012-terraform-state"

# # Health check configuration
# health_check_path               = "/health"
# health_check_interval           = 30
# health_check_timeout            = 5
# health_check_healthy_threshold  = 2
# health_check_unhealthy_threshold = 2
# health_check_matcher            = "200"

# # Session stickiness
# enable_stickiness               = false

# # HTTPS configuration
# enable_https                    = false
# enable_https_redirect           = false
# certificate_arn                 = ""
# ssl_policy                      = "ELBSecurityPolicy-TLS-1-2-2017-01"

# # Listener rules
# path_patterns                   = []

# # CloudWatch alarms
# latency_threshold               = 2.0

# =============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATIONS
# =============================================================================

# Development environment (terraform.tfvars)
# - Smaller resources (task_cpu = "256", task_memory = "512")
# - Fargate Spot for cost savings
# - Single instance (desired_count = 1)
# - No auto-scaling
# - Shorter log retention (7 days)

# Staging environment (staging.tfvars)
# - Medium resources (task_cpu = "512", task_memory = "1024")
# - Fargate Spot
# - Multiple instances (desired_count = 2)
# - Optional auto-scaling
# - Medium log retention (14 days)

# Production environment (prod.tfvars)
# - Full resources (task_cpu = "1024", task_memory = "2048")
# - Fargate (not Spot)
# - Multiple instances (desired_count = 3+)
# - Auto-scaling enabled
# - Long log retention (30 days)
# - HTTPS enabled with ACM certificate
